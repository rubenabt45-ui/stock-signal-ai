
# Supabase Configuration (Required)
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Financial Data API (Optional - for enhanced market data)
VITE_FMP_API_KEY=your_financial_modeling_prep_api_key

# Stripe Configuration (Required for payments)
VITE_STRIPE_SANDBOX=false

# Production Notes:
# - All VITE_ prefixed variables are safe for client-side use
# - Server-side secrets (STRIPE_SECRET_KEY, OPENAI_API_KEY, etc.) are configured in Supabase Edge Functions
# - Set VITE_STRIPE_SANDBOX to 'true' for development, 'false' for production
# - Remove any test/development keys before production deployment
