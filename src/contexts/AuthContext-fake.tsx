// Re-export fake auth for compatibility
export { FakeAuthProvider as AuthProvider, useFakeAuth as useAuth } from '@/providers/FakeAuthProvider';
export type { FakeAuthContextType as AuthState } from '@/providers/FakeAuthProvider';
